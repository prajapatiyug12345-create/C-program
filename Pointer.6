#include <stdio.h>

int main() {
    char str[] = "Hello";
    
    char *p_char = str; 

    printf("Characters in the array:\n");
    
    
    while (*p_char != '\0') {
        printf("%c", *p_char); 
        
        p_char++; 
    }
    printf("\n");
    
    return 0;
}
