#include <stdio.h>

int main() {
    int var = 42;
    // Declare an integer pointer
    int *ptr;

    // Make the pointer point to the variable
    ptr = &var;

    // Print the value using the pointer (dereferencing)
    printf("The value of the variable is: %d\n", *ptr);
    
    return 0;
}
