#include <stdio.h>

int main() {
    int num = 99;
    int *p = &num;
    printf("Address stored in the pointer (p): %p\n", (void*)p);

    printf("Value at the address (*p): %d\n", *p);

    return 0;
}
