#include <stdio.h>

int main() {
    int value = 100;
    int *p = &value;

    printf("Original value: %d\n", value);
    *p = 500;

    printf("Updated value using pointer: %d\n", value);

    return 0;
}
