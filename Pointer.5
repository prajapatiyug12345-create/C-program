#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    
    int *ptr = arr; 

    
    int third_element = *(ptr + 2); 

    printf("The third element (index 2) is: %d\n", third_element);
    
    return 0;
}
